import { Currency } from "../settings/Currency";
export interface EventSettings {
    shareMedia: boolean;
    splitBills: boolean;
    enableChats: boolean;
    allowCheckList: boolean;
    currency: Currency;
}
